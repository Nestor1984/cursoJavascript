
// Arrays - Arreglos
const arreglo = [1, 10, 5]; // Asi definimos un arreglo
console.log(arreglo);

// Objeto
const Persona = {
    nombre: 'Carlos',
    edad: 27
};

console.log(Persona);
console.log(Persona.nombre); // Con el punto accedemos al atributo

// Function
function hola(){ // Asi se declara un metodo en javascript
    console.log("Hola");
}

hola(); // Invocamos al metodo hola

// Null
const miVariable = null;

// Undefined
const miVariable2 = undefined;